<Window x:Class="SpellForge.Views.Dialogs.ModEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Custom Modifier"
        Width="380" Height="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Name -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Modifier Name" Foreground="{StaticResource DimBrush}"
                       FontSize="10" Margin="0,0,0,2"/>
            <TextBox x:Name="NameBox" FontSize="12"/>
        </StackPanel>

        <!-- Category / Cost / Max -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="60"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Category" Foreground="{StaticResource DimBrush}"
                           FontSize="10" Margin="0,0,0,2"/>
                <ComboBox x:Name="CatBox" SelectedIndex="0">
                    <ComboBoxItem Content="Range"/>
                    <ComboBoxItem Content="Duration"/>
                    <ComboBoxItem Content="Origin"/>
                    <ComboBoxItem Content="Targets"/>
                    <ComboBoxItem Content="Shape"/>
                </ComboBox>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Cost (pts)" Foreground="{StaticResource DimBrush}"
                           FontSize="10" Margin="0,0,0,2"/>
                <TextBox x:Name="CostBox" Text="1" TextAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="4">
                <TextBlock Text="Max buys" Foreground="{StaticResource DimBrush}"
                           FontSize="10" Margin="0,0,0,2"/>
                <TextBox x:Name="MaxBox" Text="1" TextAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Description label -->
        <TextBlock Grid.Row="2" Text="Description" Foreground="{StaticResource DimBrush}"
                   FontSize="10" Margin="0,0,0,2"/>

        <!-- Description text area -->
        <TextBox Grid.Row="3" x:Name="DescBox"
                 AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontSize="10" Margin="0,0,0,10"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="72" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Add"    Width="72" Click="OK_Click"/>
        </StackPanel>
    </Grid>
</Window>
