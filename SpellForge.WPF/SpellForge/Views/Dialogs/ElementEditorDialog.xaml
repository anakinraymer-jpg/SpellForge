<Window x:Class="SpellForge.Views.Dialogs.ElementEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Element"
        Width="440" Height="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Modification line -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Modification Line  (short — shown on element row)"
                       Foreground="{StaticResource DimBrush}" FontSize="10" Margin="0,0,0,2"/>
            <TextBox x:Name="ModBox" FontSize="11"/>
        </StackPanel>

        <!-- Description label -->
        <TextBlock Grid.Row="2" Text="Description" Foreground="{StaticResource DimBrush}"
                   FontSize="10" Margin="0,0,0,2"/>

        <!-- Description text area -->
        <TextBox Grid.Row="3" x:Name="DescBox"
                 AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontSize="10" Margin="0,0,0,10"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Reset" Width="72" Margin="0,0,8,0" Click="Reset_Click"
                    ToolTip="Revert to default text"/>
            <Button Content="Cancel" Width="72" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Save"   Width="72" Click="OK_Click"/>
        </StackPanel>
    </Grid>
</Window>
